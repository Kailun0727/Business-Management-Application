<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BMA">
        <activity android:name=".AdminMenuFragment"></activity>
        <activity android:name=".UserMenuFragment" />
        <activity
            android:name=".AddProductPage"
            android:parentActivityName=".ProductListActivity" />
        <activity
            android:name=".UpdateProduct"
            android:parentActivityName=".ProductListActivity" />
        <activity
            android:name=".ProductListActivity"
            android:exported="true" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".UserLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>